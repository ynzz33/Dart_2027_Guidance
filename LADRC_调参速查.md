# LADRC 调参速查（先用在 Roll）

> 2026/06/21 把原非线性 ADRC 换成 **LADRC（线性自抗扰，高志强带宽法）**。文件名仍是 `imcalib/Tool/adrc.c` / `adrc.h`（没改名，免动 eide/MDK 工程）。
> 原 `ADRC_调参速查表.md` / `ADRC_使用指南.md` 是旧非线性版，**已过时**，别按那个调。

---

## 一句话原理

把镖体单轴当成二阶系统 `角加速度 = 总扰动 + b0·输出`。

1. **LESO（观测器）** 只看"测到的角度"和"上拍输出"，就估出三样东西：
   - `z1` ≈ 角度
   - `z2` ≈ 角速度（不靠微分，天生干净）
   - `z3` ≈ **总扰动**（气动、舵面耦合、重心偏、模型误差……全算一起）
2. **LSEF（控制律）**：`u0 = kp·(目标−z1) − kd·z2`
3. **扰动补偿**：`u = (u0 − z3) / b0` → 把估出来的扰动直接减掉，再限幅输出。

好处：不要精确模型，自动顶掉一切扰动；**整个轴只有 3 个旋钮**。

---

## 3 个旋钮（只调这仨，其它自动算）

位置：`ladrc_ctrl[LADRC_ROLL].xxx`，调试器 Watch 在线改，**改完下一拍即生效**。

| 旋钮 | 含义 | 调大 | 调小 | Roll 默认 |
|------|------|------|------|-----------|
| `wc` | 控制带宽（跟多快） | 更快更冲，过大发抖 | 更软更慢 | **5** |
| `wo` | 观测带宽（看多快） | 估计更快，过大吃噪声发毛刺 | 估计迟钝 | **20**（=4×wc）|
| `b0` | 控制增益估计（整体力度） | 输出变**软** | 输出变**猛**，过小会发散 | **20** |

口诀：**`wo` 跟着 `wc` 走（3~5 倍）；先调 `wc` 定快慢，再调 `b0` 定力度。**

---

## Roll 整定步骤（台架）

> 当前 `ladrc_mode = 3`：pitch/yaw 还是 PID，**只有 roll 跑 LADRC**。输出限幅 ±15°（和原 PID 内环一致），台架上转不飞，安全。

1. **先确认方向对**：手动把镖体 roll 拨开一点，看舵机是否往**回正**方向打。
   - 若往**反方向**（越打越歪）→ 说明力度符号反了，把 `b0` 改成**负值**（如 `-20`）再试。
   - （正常不会反，因为和原 PID 同号；但 b0 符号弄错是唯一会立刻发散的情况，先验证。）
2. **调 `b0` 定力度**（`wc=5, wo=20` 不动）：
   - 回正太**慢/软** → `b0` **减半**（20→10→5…）。
   - 一抖就**震荡/嗡嗡** → `b0` **加倍**（20→40…）。
   - 目标：拨开后干脆地回正，**不超调或微超调**。
3. **调 `wc` 定快慢**（`b0` 定好后）：
   - 想更跟手 → `wc` 加大（5→7→10…），**同时** `wo` 跟着改成 `4×wc`。
   - 发抖/啸叫 → `wc` 调回小一点。
4. **`wo` 微调**：默认 `4×wc` 够用。`z2/z3` 毛刺大就把 `wo` 往小压一点；扰动顶不住、回正发飘就往大抬一点。
5. **死区** `deadband=1.0°`：静止时舵机还在小幅抖就适当加大（如 1.5）；想更精细就减小。

---

## Vofa 必看的几个量（验证观测器是否工作）

> 加 Vofa 通道前**先核对 `TotalControl.c` 的 `Vofa()` 下标**（老规矩）。

- `ladrc_ctrl[LADRC_ROLL].z1` vs 实际 roll 角 → 应**重合**（z1 跟得上角度）。
- `ladrc_ctrl[LADRC_ROLL].z2` vs `Surface.current_gyro_Euler[NOW][ROLL]`（实测角速度）→ 应**重合**。**这是观测器好不好用的金标准**：z2 能跟上实测陀螺，说明 LESO 正常。
- `ladrc_ctrl[LADRC_ROLL].z3` → 总扰动估计。稳态应趋于一个**平的非零值**（顶住重力/气动偏置）；乱跳说明 `wo` 太大或 `b0` 太离谱。
- `ladrc_ctrl[LADRC_ROLL].u` → 最终输出（=送混控的 roll 力矩需求，±15 内）。
- `ladrc_ctrl[LADRC_ROLL].error` → 目标−当前。

---

## 一键回退 / 切换

`surface_control_task.c` 顶部全局 `ladrc_mode`：

- `0` → **全 PID**（出问题立刻改 0，回到原来能打的状态）
- `3` → **仅 roll 用 LADRC**，pitch/yaw 仍 PID（当前默认，本次要试的）
- `1` → 三轴全 LADRC（roll 调好后再说；注意 pitch 在 Stable 段会被主动控，和 PID 的"只阻尼"不同）

pitch/yaw 的 LADRC 参数已在 `LADRC_Init` 里占位初始化好（`wc=6,wo=24,b0=20`），等 roll 验证完照同样步骤整定。
