# AGENTS.md — 跨 AI 工作规则与约束

> **这是什么**：给**任何** AI 助手（Claude / Cursor / Copilot / Codex / Gemini …）的统一工作契约。
> 接手本仓库（或本作者的任何项目）前**先读完这一篇**，再动手。目标：少返工、对齐偏好、高效率。
> **怎么用**：§1–§8 是**通用规则**（可整段复制到作者的其它项目）；文末「本项目入口」是**项目特定**部分，换项目时替换。
> AGENTS.md 是跨 AI 事实标准；Claude Code 另见 [CLAUDE.md](CLAUDE.md)（薄指针，指回本文）。

---

## §1 沟通方式（最高优先级）

- **全程中文。**
- **作者非技术背景**：描述的是**现象和想要的效果**（"打不准""一直抖""yaw 不稳"），不一定用准术语。你的活儿是**用产品思维把"想要的效果"翻译成技术动作**，而不是要求作者讲清技术细节。
- **先对齐目标再动手**：拿不准就用一两句话复述"你是想达到 X 效果对吧"，确认后再干；**不要**把一堆技术选项甩给作者让 TA 选。
- **给结论 + 推荐，不要选项清单堆砌**：需要决策时给出你推荐的那个 + 一句理由，而不是罗列 3 个方案让 TA 权衡。
- **脾气大 → 干脆利落**：少废话、少铺垫、直接上结论和结果。做完了就说做完了；做不了/没验证就直说，别含糊。

## §2 接手任何项目的第一步

1. **先读项目地图**：本项目是 [CODE_OVERVIEW.md](CODE_OVERVIEW.md)（代码地图）+ [PROGRESS.md](PROGRESS.md)（进度/决策/TODO）。**答题、改代码前必读**，别靠猜。
2. 没有地图文档的项目 → **先建**（一篇 CODE_OVERVIEW 等价的代码地图 + 一篇 PROGRESS 等价的进度），这是后续所有 AI 高效协作的地基。
3. 读完地图再看具体代码；**地图与代码冲突时以代码为准**，并顺手修地图。

## §3 改动哲学

- **外科手术式最小改动**：只动该动的，别顺手重构无关代码。新代码风格、命名、注释密度**贴合周围既有代码**。
- **少建变量、用结构体聚合（作者明确要求）**：不要散落一堆琐碎的单独变量；**只保留必须的**。相关的多个变量**归进 typedef 结构体**统一命名、统一访问（风格如本项目 `IMU_Data_t IMU_Data`、`Surface_t Surface`）。**尤其别为了"方便 Vofa 观测/调试"就新增一堆观测变量**——要观测时复用已有结构体字段或临时手段即可，不留长期垃圾全局。
- **保留可回退的对照开关**：上新方案时**不要删旧路径**，用运行时开关（全局 flag / 宏 / 调试器 Watch 变量）把新旧并存、能在线 A/B 切换。本项目范例：`Alloc_Mode`、`ladrc_mode`、`pitch_glide_mode`、`PNG_Mode`、各种 `*_Flag`。
- **一次只改一个变量**：作者在持续调参，改动要可归因。别一把改一堆东西让 TA 没法判断是哪条起了作用。
- **单位 / 坐标系 / 轴向是嵌入式控制的头号 bug 源**：改任何涉及姿态、力矩、符号、量纲的代码前，**先确认约定**（本项目坐标系见 CODE_OVERVIEW §5），别想当然。

## §4 控制 / 算法方案偏好（控制类项目通用，作者既定原则）

提控制优化方案时默认遵守：

1. **保留串级结构，不退单环**（角度环 → 角速度环）。
2. **滤波在输入端/传感器融合解决，不在输出端/反馈环内加低通**。
3. **"更好的输入"靠融合（Mahony/卡尔曼/EKF），不是更重的低通**。

**为什么**：重低通虽平滑但相位滞后大，滞后是闭环震荡主因、降增益压不住；纯 P 串级在数学上≈单环 PD + 陀螺阻尼（内环 P 即阻尼、本就"用角速度预判"），抗扰更强；反馈环内低通＝又引一个延迟源。
**红线推论**：**不要用"加重低通 / 降增益"去掩盖震荡**——找根因（滞后源、坐标系不一致、微分冲击、饱和砍轴…），从输入端/源头/结构治。

## §5 参数与文档纪律

- **作者的项目常处于高频调参期**（几乎每个 commit 都在改增益/阈值/档位）。**文档只记结构、方案、"为什么"，不追具体数值**——所有参数**以代码为准**，文中数值一律视为"某时刻快照、可能已过时"。
- **改了代码 → 同步文档**：CODE_OVERVIEW（地图）改对应小节、PROGRESS（进度）补一条时间线。两篇 + 作者的 AI memory 三方保持一致。
- **删文件 → 修引用**：删 md/源文件后，把指向它的链接一并改掉（标"已删除/内容并入 X"），别留死链。

## §6 验证现实与诚实边界

- **AI 通常编不了 / 烧不进**：本项目是 **Keil/MDK** 工程，需在 IDE 里编译下载。**绝不**声称"已编译/已验证/已修复生效"你没真做的事。改完标注 **未编译 / 待台架 / 待实测**。
- **能做的核验要做、并标清**：纯数学/几何推导、数值仿真（Python 算一遍零空间、符号、收敛性）是有价值的，**做了就说"已数值核验"**；但**物理符号、增益、轴向正负只能上台架/试飞定**——明确区分「已核验」vs「待硬件」。
- **调参/观测靠上位机**：本项目用 **Vofa+**（UART 遥测）+ 调试器 Watch。
  - **显示值不对，先查打包/下标，再怀疑算法**：Vofa 通道发的是哪个变量、下标对不对（见 `TotalControl.c` 的 `Vofa()`），核对完再去查控制律。这是反复踩过的坑。
- **如实报告**：测试失败就贴失败输出说失败；某步跳过了就说跳过；做完并验证了才平直地说"完成"，不夸大、不含糊。

## §7 红线（未经明确许可不要做）

- ❌ 不臆改物理符号 / SIGN / 轴向正负（只能台架标定，乱翻会炸机）。
- ❌ 不用低通 / 降增益掩盖震荡（见 §4）。
- ❌ 不一次性大重构 / 大批量改参（见 §3）。
- ❌ 不删除你没创建、或与描述不符的文件/代码——先停下来说明，别径直删。
- ❌ 不做对外发送 / 不可逆操作（推送、发布、删数据）前不确认。
- ❌ 不声称未实际完成的编译/验证/硬件结果（见 §6）。

## §8 跨项目适配（把本文复制到新项目时）

1. **§1、§3、§5、§6、§7 基本通用**，直接保留。
2. **§4** 是控制类项目偏好——非控制项目可删/换成该项目的领域偏好。
3. **替换文末「本项目入口」**：指向新项目的地图文档、构建/烧录方式、上位机/调试手段、当前主线状态。
4. 放在仓库根目录命名 `AGENTS.md`（多数 AI 工具自动识别）；用 Claude Code 再加一个薄 `CLAUDE.md` 指回本文。

---

## 本项目入口（Dart_2027 飞控 · 项目特定，换项目时替换）

- **一句话**：STM32G431 + BMX055 + FreeRTOS 的 **RoboMaster 飞镖 X 翼飞控**——串级 PID（+可选 LADRC）自稳 + 视觉末制导，4 舵面 X 翼，Mahony 姿态 + 视觉/IMU 紧耦合 EKF 速度。
- **地图文档**：[CODE_OVERVIEW.md](CODE_OVERVIEW.md)（环境/任务/数据流/坐标系/状态机/分配器/全局/陷阱）、[PROGRESS.md](PROGRESS.md)（时间线/决策/TODO）。**先读这两篇。**
- **核心代码**：`imcalib/Task/`（IMU.c 姿态、surface_control_task.c 制导+混控、CallBack_Task.c 视觉/中断、PNG_Task.c 比例导引）、`imcalib/Tool/`（pid.c、adrc.c、vision_ins.c EKF、filter.c）。
- **构建/验证**：Keil MDK（`MDK-ARM/*.uvprojx`），**AI 环境编不了**→改完标"未编译"；台架靠 **Vofa+** 遥测 + 调试器 Watch。
- **坐标系**（极易混）：机体系右手 ENU（X=右/Y=前/Z=上）；陀螺轴序非标准 ZYX（`gx=pitch/gy=roll/gz=yaw`）。改姿态/力矩前看 CODE_OVERVIEW §5。
- **当前主线状态**（快照，以代码为准）：三轴**直通**串级 PID（`ladrc_mode=0`、欧拉运动学变换已撤回作 TODO）；调参哲学＝**内环>外环纯 P**；分配 `Alloc_Mode=2`（最小能量）；末制导 pitch＝**主动滑翔→扎** `pitch_glide_mode=1`；PNG/PN 超前、速度矢量追踪、LADRC、LPF 平滑均**就绪但未启用**（`#if 0` / mode=0 / 注释）。
- **作者 AI memory（Claude）**：用户画像、Vofa 打包先查、速度 EKF、LADRC——与本文 §1/§4/§6 同源，更新一处时留意另一处。

---
*维护：本文通用部分（§1–§8）改动应同步到作者其它项目的 AGENTS.md；本项目入口随代码主线变化更新。*
