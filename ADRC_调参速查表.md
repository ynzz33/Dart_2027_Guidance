# ADRC调参速查表

## 🚀 快速上手（3步）

### 第1步：初始化
```c
// 在 main.c 或初始化函数中
#include "adrc.h"
ADRC_Init_All();    // 初始化所有ADRC
adrc_enable = 1;    // 使能ADRC
```

### 第2步：替换PID
```c
// 在 surface_control_task.c 中
// 原来：Euler_pid_Cale(delta_time);
// 改为：
if (adrc_enable)
    Euler_ADRC_Cale(delta_time);
else
    Euler_pid_Cale(delta_time);
```

### 第3步：调参
从ESO带宽开始调，按顺序：`w0` → `r` → `wc` → `b0`

---

## 📊 参数速查表

### 角度环（外环）

| 参数 | 含义 | Pitch | Roll | Yaw | 调整方向 |
|------|------|-------|------|-----|---------|
| `td.r` | 跟踪速度 | 100 | 100 | 80 | ↑快 ↓慢 |
| `td.h0` | 滤波因子 | 0.005 | 0.005 | 0.005 | ↑平滑 ↓敏感 |
| `eso.w0` | ESO带宽 | 15 | 20 | 20 | ↑快 ↓稳 |
| `eso.b0` | 控制增益 | 1.0 | 1.0 | 1.0 | ↑强 ↓弱 |
| `nlsef.wc` | 控制带宽 | 8 | 10 | 10 | ↑快 ↓稳 |
| `deadband` | 死区(°) | 0.5 | 0.5 | 0.5 | ↑稳 ↓敏 |

### 角速度环（内环）

| 参数 | 含义 | Pitch | Roll | Yaw | 调整方向 |
|------|------|-------|------|-----|---------|
| `td.r` | 跟踪速度 | 300 | 300 | 250 | ↑快 ↓慢 |
| `td.h0` | 滤波因子 | 0.002 | 0.002 | 0.002 | ↑平滑 ↓敏感 |
| `eso.w0` | ESO带宽 | 60 | 80 | 80 | ↑快 ↓稳 |
| `eso.b0` | 控制增益 | 1.0 | 1.0 | 1.0 | ↑强 ↓弱 |
| `nlsef.wc` | 控制带宽 | 30 | 40 | 40 | ↑快 ↓稳 |
| `deadband` | 死区 | 0 | 0 | 0 | 通常为0 |

---

## 🎯 调参口诀

```
ESO带宽定基调，先小后大慢慢调；
TD速度管跟踪，太快会抖太慢滞；
NLSEF带宽管响应，振荡就减慢就加；
b0大小管补偿，力度不够就增大。
```

---

## 🔍 观测指南

### 必须观测的变量（Vofa）

```c
// 添加到Vofa Watch
float observe[7] = {
    adrc_ctrl[2][0].target,      // 目标角度
    adrc_ctrl[2][0].feedback,    // 当前角度
    adrc_ctrl[2][0].error,       // 误差
    adrc_ctrl[2][0].td.x1,       // 平滑目标
    adrc_ctrl[2][0].eso.z1,      // 角度估计
    adrc_ctrl[2][0].eso.z2,      // 角速度估计
    adrc_ctrl[2][0].eso.z3       // 扰动估计
};
```

### 正常现象

| 变量 | 应该看到 |
|------|---------|
| `z1` | ≈ 实际角度，比测量更平滑 |
| `z2` | ≈ 实际角速度 |
| `z3` | 能跟踪扰动，有合理幅值 |
| `td.x1` | 平滑过渡到目标，不突变 |

### 异常诊断

| 现象 | 原因 | 解决 |
|------|------|------|
| z1/z2 抖动厉害 | ESO带宽太大 | `w0` ↓ |
| z1/z2 滞后严重 | ESO带宽太小 | `w0` ↑ |
| z3 一直为0 | ESO没工作 | 检查b0和alpha |
| z3 剧烈振荡 | alpha/delta不对 | `delta` ↑ |
| 控制输出抖 | 多个参数都可能 | 见下方 |
| 响应太慢 | 带宽太小 | `w0` ↑ `wc` ↑ |
| 稳态误差 | b0不对或死区大 | `b0` 调整，`deadband` ↓ |

---

## ⚡ 快速修复

### 问题1：输出抖动

```c
// 方案A：减小ESO带宽
ESO_SetBandwidth(&eso, 10.0f);  // 从20减到10

// 方案B：增大fal的delta
eso.delta = 0.05f;      // 从0.01增到0.05

// 方案C：减小TD速度
td.r = 50.0f;           // 从100减到50

// 方案D：对反馈滤波（如果有噪声）
feedback = KalmanFilter(&kf, feedback, 0.1f, 5.0f);
```

### 问题2：响应慢

```c
// 方案A：增大ESO带宽
ESO_SetBandwidth(&eso, 30.0f);  // 从15增到30

// 方案B：增大NLSEF带宽
NLSEF_SetBandwidth(&nlsef, 15.0f);  // 从8增到15

// 方案C：增大b0
eso.b0 = 1.3f;         // 从1.0增到1.3

// 方案D：增大TD速度
td.r = 150.0f;         // 从80增到150
```

### 问题3：稳态误差

```c
// 方案A：调整alpha
eso.alpha2 = 0.3f;     // 从0.5减到0.3

// 方案B：减小delta
eso.delta = 0.005f;    // 从0.01减到0.005

// 方案C：调整b0
eso.b0 = 1.2f;         // 尝试1.0~1.5

// 方案D：减小死区
deadband = 0.3f;       // 从0.5减到0.3
```

### 问题4：目标跳变时超调

```c
// 方案A：减小TD速度
td.r = 50.0f;          // 从100减到50

// 方案B：增大TD滤波
td.h0 = 0.01f;         // 从0.005增到0.01

// 方案C：减小NLSEF带宽
NLSEF_SetBandwidth(&nlsef, 8.0f);  // 从12减到8
```

---

## 📈 调参流程图

```
开始
  │
  ▼
┌─────────────────┐
│ 1. 用默认参数跑  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 2. 观察z3是否正常│◀─────────────────┐
└────────┬────────┘                  │
         │                           │
    ┌────┴────┐                      │
    ▼         ▼                      │
  正常      异常                      │
    │         │                      │
    ▼         ▼                      │
┌───────┐ ┌─────────────┐           │
│继续   │ │调整w0和alpha │───────────┘
└───┬───┘ └─────────────┘
    │
    ▼
┌─────────────────┐
│ 3. 测试阶跃响应  │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
  满意      不满意
    │         │
    ▼         ▼
┌───────┐ ┌─────────────┐
│完成！ │ │调整wc和r    │
└───────┘ └──────┬──────┘
                 │
                 ▼
          回到步骤3
```

---

## 🎓 进阶技巧

### 1. 自适应参数

```c
// 根据误差动态调整
float error = fabsf(adrc->error);
if (error > 20.0f) {
    ESO_SetBandwidth(&adrc->eso, 30.0f);  // 大误差：激进
    adrc->eso.b0 = 1.3f;
} else if (error < 5.0f) {
    ESO_SetBandwidth(&adrc->eso, 20.0f);  // 小误差：保守
    adrc->eso.b0 = 1.0f;
}
```

### 2. 分阶段参数

```c
switch (Guidance_State) {
    case Stable:
        ESO_SetBandwidth(&eso, 15.0f);  // 自稳：保守
        break;
    case Terminal:
        ESO_SetBandwidth(&eso, 25.0f);  // 制导：激进
        break;
}
```

### 3. 单通道测试

```c
// 只对Yaw用ADRC，其他用PID
if (channel == YAW)
    output = ADRC_Calc(...);
else
    output = pid_calc(...);
```

---

## 📝 调参记录模板

```
日期：______
通道：Yaw 角度环

初始参数：
- w0 = 15
- r = 80
- wc = 10
- b0 = 1.0

测试现象：
- 响应：□快 □慢 □合适
- 稳定性：□抖 □稳 □振荡
- 稳态误差：□有 □无

调整：
- w0: 15 → __
- r: 80 → __
- wc: 10 → __
- b0: 1.0 → __

结果：
- 响应：□快 □慢 □合适
- 稳定性：□抖 □稳 □振荡
- 稳态误差：□有 □无

下一步：
_________________________________
```

---

## ✅ 最终检查清单

- [ ] ADRC已正确初始化
- [ ] adrc_enable已设置为1
- [ ] ESO带宽w0已调好（观察z3）
- [ ] TD速度r已调好（观察跟踪）
- [ ] NLSEF带宽wc已调好（观察响应）
- [ ] b0已调好（观察补偿效果）
- [ ] 无稳态误差
- [ ] 无抖动
- [ ] 阶跃响应满意
- [ ] 与PID对比效果更好

**完成以上所有检查，ADRC调参成功！** 🎉
