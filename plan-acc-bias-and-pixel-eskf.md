# 加速度零偏重做与像素角 ESKF 计划

> 状态：**第一阶段+第二阶段代码已改（2026-08-20），未编译，待台架验证**。
>
> 已确认：`Shot_Pitch/Roll` 是真实发射架机械角，正负正确；上电后可静置完成校准；装弹期间仍属于发射前状态。
>
> 范围：先解决 `IMU_Data.A_Offset` 的标定/使用逻辑；之后才评估只用视觉方位角的 ESKF。
> 两阶段必须分开提交、分开台架验收，不能一次重写姿态、速度和制导链路。

---

## 1. 目标与边界

### 1.1 第一阶段目标：加速度零偏

当前上电静态标定先用含零偏的加速度驱动 Mahony，再计算：

```text
A_Offset = mean(a_meas) - R_col3_from_Mahony
```

这里的 `R_col3` 本身由同一份含零偏加速度决定。横向零偏会先被 Mahony 解释为 pitch/roll 倾角，再被这条公式抵消，结果仅在当前摆放姿态下自洽，不能得到可用于飞行的三轴物理零偏。

新方案使用发射架已知姿态 `Shot_Pitch`、`Shot_Roll` 直接构造理论静止重力，再从静置均值中减去它。yaw 绕重力轴旋转，不影响重力在机体系中的投影，因此不需要磁力计或可靠 yaw。

第一阶段不改变：

- `ACC_SIGN_*`、`GYR_SIGN_*`、IMU 轴映射、控制器符号；
- LQI、舵面分配、制导状态机目标角逻辑；
- 加速度滤波强度，不用低通或降增益掩盖问题；
- 视觉/EKF 的结构和输出。

### 1.2 第二阶段目标：仅用视线角的真正 ESKF

视觉量测可以直接使用已经换算好的 `Vision_Rx_Data.Euler[NOW][PITCH/YAW]`。它与原始像素是一一对应的固定比例换算，使用角度更适合滤波器；进入滤波器前统一转成弧度。

没有距离时，视觉只提供两维方位/俯仰量测：

- 可修正视线方向、横向位置/速度误差、部分姿态误差；
- 不能直接观察目标距离和沿视线速度；
- 因此不能把该模式输出当作可靠绝对速度、`range_m`、`V_c` 或距离调度依据；
- 当前末制导的视觉跟踪和 PNG 视线率主要需要角度/角速度，距离不是前置条件。

第二阶段必须并行运行，不得直接接管 `IMU_Data.Velocity` 或控制链路。

---

## 2. 实现前必须确认的事实

以下事实已经由作者确认，可作为新零偏方案的前提。

1. `Shot_Pitch` 和 `Shot_Roll` 是发射架的真实机械姿态，而不是根据现有 IMU 输出反复调出来的状态机阈值。
2. 正负约定正确：`Shot_Pitch>0` 表示镖头上抬；`Shot_Roll>0` 表示工程定义的右滚。不得仅凭感觉翻符号。
3. 上电后的现有 3 秒校准窗口内，飞镖在发射架上保持静止。
4. 上电到发射之间是否可能被拿起、搬动、装回发射架。该事实决定 ZUPT 的“已发射锁存”如何设置。
5. 相机相对机体系的安装姿态、光轴是否已知。第二阶段若不知道相机外参，不能宣称角度 ESKF 的轴向已正确。

若以后更换发射架或修改 `Shot_Pitch/Roll`，必须重新上电完成一次静态标定。允许根据 IMU 反馈微调发射架角度，但微调依据必须是实际机械姿态/外部参照；不能使用未校正 Mahony 的初始输出反过来定义 `Shot_Pitch/Roll`。

---

## 3. 第一阶段详细设计：发射架姿态零偏

### 3.1 理论重力向量

现有机体系为右手 ENU：`X=右`、`Y=前`、`Z=上`；静止加速度计应读到朝上的 `+1g`。在发射架姿态下，理论重力/静止比力在机体系为：

```text
pitch = Shot_Pitch * DEG2RAD
roll  = Shot_Roll  * DEG2RAD

g_ref_body[X] = -sin(roll)
g_ref_body[Y] =  cos(roll) * sin(pitch)
g_ref_body[Z] =  cos(roll) * cos(pitch)
```

这个向量不含 yaw。实现前应以当前四元数/旋转矩阵约定做纯数值核对：当 `roll=0,pitch>0` 时，`Y` 分量为正；当 `pitch=0,roll>0` 时，`X` 分量为负。核对只验证公式，不改任何 SIGN 宏。

### 3.2 最小改动与旧路径保留

直接复用现有 `IMU_Data.A_Offset[3]`，不增加第二套偏置数组、不改 IMU 主结构布局。新公式标定出的三轴偏置直接写入这个字段，并沿用现有所有消费者。

直接替换旧公式，不做 A/B 宏切换。旧公式和在线 refine 代码已删除。

### 3.3 上电静态标定流程

保持现有时间预算，不增加用户采样动作：

1. `calib_done=0`，偏置不参与计算。
2. 前 1000 ms：保持现有传感器读取、姿态运行和滤波器预热；不累积零偏。
3. 后 2000 ms：累积 `IMU_Data.A[NOW][X/Y/Z]` 与陀螺原始读数的均值。
4. 计算陀螺偏置：保持现有 `G_Offset = mean(gyro)`。
5. 根据 §3.1 计算 `g_ref_body`。
6. 计算零偏：

```text
A_Offset[i] = mean(a_meas[i]) - g_ref_body[i]
```

7. `calib_done=1`。之后新模式偏置固定到下一次上电，绝不被“静止在线 refine”改写。

旧公式只是回退对照，不能再被描述为可靠三轴零偏。

### 3.4 校准后统一数据流

数据流统一用去偏值，避免”姿态用裸值、积分用去偏值”的自相矛盾：

```text
a_meas (g)
  -> a_corr = a_meas - A_Offset
  -> Mahony: normalize(a_corr)，计算加速度可信度与重力校正
  -> ZUPT 静止判据：abs(norm(a_corr) - 1g)
  -> A_World: gravity * (a_corr - R_col3)
  -> VisInsEKF_Predict(a_world)
```

`a_corr` 喂给 Mahony 是必要的。否则横向零偏虽从 `A_World` 扣掉，仍会在 Mahony 中被当成初始倾角，后续旋转矩阵依然错误。

### 3.5 ZUPT 与零偏冻结

新模式下：

- ZUPT 只做 `v=0` 的量测更新，不更新新公式得到的 `A_Offset`；
- 新偏置在 `IMU_Calibrate()` 完成后立即冻结；
- `ACC_BIAS_LPF_K` 在线 refine 只允许在旧宏路径生效，以保留对照。

现代码注释写“发射后停 ZUPT”，但实际调用以 `imu_is_static` 为条件。实现者需单独处理这个问题：飞行段即使恰好出现 `|a|≈1g` 且角速度小，也不允许误做 `v=0` 更新。

本项目已确认装弹过程仍是发射前状态，因此采用更小的状态机门控：只在 `Guidance_State==Self_Text_State || Guidance_State==Start` 时允许 ZUPT；进入 `Stable` 后停止 ZUPT，随后 `Terminal/End` 也保持停止。装弹带来的短时运动会让静止判据自行失效，装好重新静置后又可继续把速度归零；但它不会改写新零偏。

### 3.6 第一阶段验收

不需要额外六面采样，但需要一次正常上电台架观察。优先用 Watch；若使用 Vofa，先核对打包下标。

观察项：

```text
IMU_Data.A_Offset[XYZ]
IMU_Data.A_World[NOW][XYZ]
IMU_Data.Euler[NOW][PITCH/ROLL]
imu_is_static
```

验收顺序：

1. 发射架静置上电：约 3 秒后新偏置固定，`A_World` 无持续单轴偏置，ZUPT 后速度接近零。
2. 同一发射架连续冷启动：新偏置和初始 pitch/roll 应重复稳定，不应因 Mahony 初始收敛不同而明显跳变。
3. 若结果明显更差，先核对 `Shot_Pitch/Roll` 的真实机械值和正负，不准改 SIGN 宏掩盖。
4. 发射后：确认偏置不再变化，且 ZUPT 不会把飞行速度错误清零。

第一阶段只完成“零偏物理定义正确、数据流一致”。它不能消除加速度计比例误差、轴不正交、温漂，也不能替代完整导航滤波器。

---

## 4. 第二阶段详细设计：仅用视线角的 ESKF

### 4.1 前置结论

可以使用转换后的角度，不必保留原始像素。将：

```text
Vision_Rx_Data.Euler[NOW][YAW]   -> az_meas (rad)
Vision_Rx_Data.Euler[NOW][PITCH] -> el_meas (rad)
```

作为视觉量测。转换前提是像素中心、FOV 和畸变已足以支持当前命中精度；角度换算不是 EKF 的非线性来源，坐标旋转和 bearing 量测方程才是。

### 4.2 推荐状态：15 状态误差状态 EKF

不要使用“协方差直接包含 4 维四元数”的全状态 EKF。推荐保存名义状态、对 15 维小误差建协方差：

```text
nominal: p(3), v(3), q(4), b_g(3), b_a(3)
error:   dp(3), dv(3), dtheta(3), dbg(3), dba(3)
```

预测：

```text
omega = gyro_meas - b_g
f_b   = acc_meas  - b_a
q     = integrate(q, omega)
v     = v + (R_b2w(q) * f_b - g_world) * dt
p     = p + v * dt + 0.5 * a_world * dt^2
```

具体“重力是加还是减”必须沿用项目当前的 specific-force 与 `R_matrix_T` 定义，经数值测试确认；不得从其他工程复制标准 NED/FRD 公式后直接套用。

### 4.3 方位/俯仰量测模型

对固定靶，先由当前名义位置、姿态和相机外参预测靶在相机/机体系内的单位视线 `u_body`，再预测：

```text
az_hat = atan2(u_body[X], u_body[Y])
el_hat = atan2(u_body[Z], sqrt(u_body[X]^2 + u_body[Y]^2))
z      = [az_meas, el_meas]
innovation = wrap(z - h(x))
```

这里 `h(x)` 对位置和姿态非线性，线性化后才得到量测雅可比 `H`，这才是本项目语境下真正的 EKF/ESKF。不能再先用固定 Mahony 姿态把角度变成世界系位置，再给 `[p,v]` 套常量 `H`。

没有距离时需要明确限制：

- 初始距离可设为宽协方差的名义值，但它不是被视觉直接测到；
- 沿 LOS 的位置和速度长期不可观，直线向靶飞行时尤其差；
- 禁止输出 `range_m`、`V_c` 供控制使用，除非未来重新加入并标定可靠距离；
- ESKF 输出初期只用于角度一致性、横向误差趋势和调试，不接 LQI、状态机阈值或 H_tau 调度。

### 4.4 实施顺序

1. 新建独立 `vision_bearing_eskf.c/.h`，不覆盖 `vision_ins.c`。
2. 使用结构体聚合名义状态、协方差、创新、门控标志和模式，不新增一堆 Vofa 全局。
3. 实现纯 IMU 预测、协方差传播、四元数归一化和数值有限性保护。
4. 实现二维 bearing 更新、角度环绕、Joseph 协方差更新和新息卡方门控。
5. 由 `Vision_Rx_Data.Vision_Recog_Cnt` 新帧触发，但要在临界区快照读取完整视觉角与对应 IMU 时间；不读 `dist_cm`。
6. 与当前 `vision_ins.c` 并行运行，初期只发布诊断结构体。
7. 通过台架和实飞对照后，才考虑以运行时模式开关选择哪个输出；旧 6 状态 KF 不删除。

### 4.5 第二阶段必须确认的输入

1. 相机光轴相对机体系是否严格是 `Y=前`；若有安装 pitch/roll/yaw，需要固定外参矩阵。
2. `Vision_Rx_Data.Euler` 的正方向是否与机体系 `X=右/Y=前/Z=上` 一致。必须做“目标右/上移动”的台架动作验证，不能猜。
3. 视觉帧到达时刻与 IMU 状态的时延是否可忽略；若不可忽略，要保存短历史并按时间回放/补偿。
4. 靶是否在世界中固定；若靶移动，当前模型不成立，必须把靶状态纳入模型或放弃绝对位置解释。

### 4.6 第二阶段验收

1. 静置目标不动：创新均值应接近零，门控不应频繁拒绝正常帧。
2. 手动使目标向右/向上：预测视线和量测角的正负一致；反了先查相机/机体外参，不改控制 SIGN。
3. 仅旋转飞镖：姿态变化应被视觉角残差纠正，四元数保持归一化，协方差对称且有限。
4. 连续运行：不得出现 NaN/Inf、协方差负对角、角度跨 ±180 度跳变。
5. 实飞前：确认该并行 ESKF 没有写入 `IMU_Data.Velocity`、`Surface.target_angle_Euler`、LQI 输入或状态机条件。

---

## 5. 文件影响清单

第一阶段预期修改：

- `imcalib/Task/IMU.h`：零偏结构、模式定义、注释。
- `imcalib/Task/IMU.c`：理论重力、静态标定、Mahony/ZUPT/A_World 数据流、发射锁存。
- `CODE_OVERVIEW.md`：更新 A_Offset/ZUPT 数据流说明。
- `PROGRESS.md`：记录根因、新旧模式、台架待验项。

第二阶段预期新增：

- `imcalib/Tool/vision_bearing_eskf.c/.h`。
- 构建配置中的新源文件入口。
- 对应的 `CODE_OVERVIEW.md`、`PROGRESS.md` 记录。

禁止改动：`ACC_SIGN_*`、`GYR_SIGN_*`、舵机/力矩分配、LQI 增益、视觉控制目标链路。任何物理符号问题只允许台架标定后改对应配置项。

---

## 6. 交付与诚实边界

执行者每完成一个阶段必须：

1. 先做静态数值核对，再提交最小代码改动。
2. 不声称已经编译或飞行验证；本项目需 eIDE/台架完成真实验证。
3. 报告代码级验证与台架待验证项，区分清楚。
4. 若新模式不通过，切回旧模式用于对照，不删除旧路径，也不同时改阈值、滤波或控制增益。
